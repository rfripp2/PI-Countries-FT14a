/* const { Country } = require("../db");
const axios = require("axios").default;

async function addCountries(req, res, next) {
  const countries = await Country.create({
    ID: "testID",
    name: "testName",
    continent: "testContinent",
    capital: "testCapital",
  });
  res.send(countries);
}

module.exports = {
  addCountries,
}; */
